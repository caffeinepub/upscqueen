// Type definitions for exam categories and languages
export type ExamCategory = 'tat' | 'tet' | 'upsc' | 'gpsc' | 'class3';
export type Language = 'english' | 'hindi' | 'gujarati';

// These types are kept for frontend use but static data has been removed
// Data is now loaded from the backend via React Query hooks
